#pragma once
#include <wx/wx.h>
#ifndef DESIGN_CONSTATNS
#define DESIGN_CONSTANTS
// Constants Sizes
const wxSize THUMBNAIL_SIZE = wxSize(120, 84); // The size of a thumbnail image

// Constatnt Colours
const wxColour BTN_BACKGROUND_COLOUR = wxColor(80, 80, 80); // the colour for a button background
const wxColour	PANEL_WIDGET_BACKGROUND_COLOUR = wxColour(50, 50, 50); // the colour for a panel background
const wxColour WINDOW_BACKGOUND_COLOUR = wxColour(204, 204, 204); // the colour for window background
const wxColour WINDOW_BRIGHT_BACKGOUND_COLOUR = wxColour(240, 240, 240); // the colour for window background
const wxColour WINDOW_BRIGHT_FOREGROUND_COLOUR = wxColour(252, 252, 252);
const wxColour ORANGE_BACKGROUND = wxColour(245, 245, 245);
const wxColour TEXT_COLOUR = wxColour(240, 240, 240); // the colour for text
const int ENTER_SPACE = 25; // enter space
const int SH_ENTER_SPACE = 15; // shift enter space
#endif